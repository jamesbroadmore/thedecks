'use client'
import Image from 'next/image'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { useState } from 'react'
import { authClient } from '@/lib/auth-client'

export function AuthForm({mode}:{mode:'sign-in'|'sign-up'}){ const router=useRouter(); const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false); const signup=mode==='sign-up'; async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const result=signup?await authClient.signUp.email({name,email,password}):await authClient.signIn.email({email,password});setLoading(false);if(result.error){setError(result.error.message||'Unable to continue');return}router.push('/');router.refresh()} return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><Image src="/decks-4music2-mark.png" width={44} height={44} alt="4{music}2"/><div><b><em>the</em> decks</b><small>BY 4{'{'}MUSIC{'}'}2</small></div></div><div><p className="eyebrow">PERFORMANCE OS</p><h1>{signup?'Create your library':'Welcome back'}</h1><p>Your private music library, decks, cues and AI assistant on every device.</p></div><form onSubmit={submit}>{signup&&<label>DISPLAY NAME<input value={name} onChange={e=>setName(e.target.value)} autoComplete="name" required/></label>}<label>EMAIL<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><label>PASSWORD<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={signup?'new-password':'current-password'} minLength={8} required/></label>{error&&<p className="auth-error" role="alert">{error}</p>}<button className="primary-action" disabled={loading}>{loading?'Please wait…':signup?'Create account':'Sign in'}</button></form><p>{signup?'Already have an account? ':'New to the decks? '}<Link href={signup?'/sign-in':'/sign-up'}>{signup?'Sign in':'Create account'}</Link></p></section></main> }
